services:
  redis:
    image: redis:7
    container_name: redis-short-url
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
